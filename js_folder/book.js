        // // Function to update the displayed width
        // function updateWidth() {
        //     const viewportWidth = window.innerWidth; // Get the current viewport width
        //     document.getElementById('width-display').innerText = 
        //         `Viewport width: ${viewportWidth}px`;
        // }

        // // Run the updateWidth function initially
        // updateWidth();

        // // Add an event listener to update the width whenever the window is resized
        // window.addEventListener('resize', updateWidth);